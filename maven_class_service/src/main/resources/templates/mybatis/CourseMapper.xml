<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pagooo.maven_class_service.course.mapper.CourseMapper">
    <sql id="selectListWhere" >
        <if test="searchType != null and searchValue != null">

            <choose>
                <when test="searchType == 'userId'">
                    where user_id like concat('%',#{searchValue},'%')
                </when>
                <when test="searchType == 'userName'">
                    where user_name like concat('%',#{searchValue},'%')
                </when>
                <when test="searchType == 'phone'">
                    where phone like concat('%',#{searchValue},'%')
                </when>
                <otherwise>
                    where
                    (
                    user_id like concat('%',#{searchValue},'%')
                    or
                    user_name like concat('%',#{searchValue},'%')
                    or
                    phone like concat('%',#{searchValue},'%')
                    )
                </otherwise>
            </choose>
        </if>
    </sql>

    <select id="selectListCount" resultType="long">
        select count(*) from course
        where 1 = 1
        <include refid="selectListWhere"></include>
    </select>


    <select id="selectList" resultType="org.pagooo.maven_class_service.course.dto.CourseDto">
        select * from course
        where 1 = 1
        <include refid="selectListWhere"></include>
        limit #{pageStart}, #{pageEnd}
    </select>
</mapper>